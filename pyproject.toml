[project]
name = "site2webmcp"
version = "0.1.0"
description = "Auto-expose webpage forms and actions as WebMCP tools for AI agents"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["site2webmcp"]
